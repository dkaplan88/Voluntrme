<div class ="header">

  <div class="h1">	
    <h1><%= link_to "Voluntr.", events_url %><span class="c"><%= link_to "me", events_url %></span></h1>
  </div>	
  
  <div class="facebook">	

	  <% if session[:volunteer_id] %>
				<% unless current == nil %>
				<p><%= image_tag current.image %> Hi <%=current.name%>! | <%= link_to "Account", "#" %></p>
				<% end %>
	  <% else %>
			<p><%= link_to "Sign up", new_volunteer_url %> | <%= link_to "log in", log_in_url  %></p>
	  <% end %>
	</div>

	<div class="clear"></div>

	<div id ="search">
  	<p><%= link_to "Contact us", "#" %></p>
  	<p><%= link_to "About us", "#" %></p> 
	</div>
</div>