<% unless current_volunteer?(@volunteer) %>
  <div id="follow_form">
  <% if current_volunteer.following?(@volunteer) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>