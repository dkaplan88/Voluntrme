<%= form_for(current_volunteer.relationships.find_by_followed_id(@volunteer),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>